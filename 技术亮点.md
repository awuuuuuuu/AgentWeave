# AgentWeave 技术亮点

记录每个关键实现决策的 Why & How。数字编号仅为索引，不代表优先级。

---

## 文档摄入（Step 1）

### 1. 分层架构：结构感知 vs. token 约束

文档处理的核心分层决策：**Parser 层负责结构感知，Splitter 层只做 token 约束**，两层职责边界清晰。

Parser 层为每种格式单独实现结构提取逻辑：
- **Word / HTML / Markdown**：递归遍历标题层级（`<h1>`、`## `、`Heading 1` 样式），每个标题节点对应一个 chunk，`section_path` 动态拼接为 `"第三章 > 3.2节"` 的层级路径
- **PDF**：提供三档模式——`fast`（PyMuPDF 按页提取，速度优先）、`smart`（逐页智能路由，见下方）、`hi_res`（全页走远端 Unstructured API，最高精度）
- **Excel**：按 Sheet 独立切块，表头单独作为 `content_type=title` 的 chunk，每行或分块作为 `content_type=table`

Splitter 层不感知文档结构，之所以能保证不跨章节/页面边界，不是因为 Splitter 知道结构，而是 Parser 输出的每个 chunk 已在一个语义单元内。这一分层使两层可以完全独立演进。

### 2. PDF smart 模式：按页三重降级路由

`smart` 模式解决 PDF 解析的核心矛盾：**本地提取速度快但无法处理图表/扫描页，远端 OCR 精度高但费时且收费**。逐页分析，只对真正需要的页面调用远端 Unstructured API：

```
第 1 页：纯文字 → fitz 本地提取（< 1ms，零成本）
第 3 页：含图表 → 单页截图 → 远端 hi_res OCR
第 5 页：空白/扫描 → 单页截图 → 远端 hi_res OCR
```

触发远端调用的三重条件（任一满足）：

| 条件 | 判断逻辑 | 典型场景 |
|------|---------|---------|
| `page_has_image` | 光栅图面积 > 页面 10%（排除 logo 等小图） | 含图表的研报、PPT 转 PDF |
| `not page_text` | 全页无可提取文字 | 扫描件、图片型 PDF |
| `_is_garbled(text)` | 不可打印字符比例 > 25% | 字体层编码损坏、CID 字体乱码 |

远端调用失败时自动退回本地 fitz 结果，不丢页、不中断 pipeline。

### 3. content_type 驱动的全链路路由

`content_type` 字段由 Parser 层写入，贯穿 Splitter → Embedder → Store 整条链路：

| content_type | Splitter 策略 | Embedder 策略 | Store 策略 |
|---|---|---|---|
| `title` | 恒定透传，不切分 | 正常 embed | 正常写入 |
| `table` | ≤ 2048 token 透传；超限强制切分 | 正常 embed | 正常写入 |
| `text` | 超出 chunk_size 时切分，保留 overlap | 正常 embed | 正常写入 |
| `error` | 透传 | 短路，返回空向量 | 跳过写入 |

路由逻辑集中在 `BaseSplitter.split()` 中，三种切分策略（Recursive / Semantic / ParentChild）继承后无需各自重复实现。

### 4. Error Chunk 错误隔离：失败不中断

解析失败时返回 `content_type="error"` 的占位 chunk，携带 `error` 字段记录原因，之后的层自动跳过它。批量处理 1000 个文件时，即使其中 5 个损坏，其余 995 个正常完成，损坏文件的 error chunk 可在 pipeline 末尾统一收集上报。

### 5. ParsedChunk 契约式 metadata 校验

所有 Parser 输出的 chunk 在 `ParsedChunk.__post_init__` 中强制校验三个必填字段（`source_file`、`content_type`、`section_path`），缺失字段在对象构造时立即报错，而非在下游运行时才发现 `KeyError`。这将"数据完整性保障"从运行时下沉到对象构造阶段——**契约式编程（Design by Contract）**思想。

### 6. RecursiveSplitter 的分隔符保留与 overlap 自适应

**分隔符保留**：按 `\n\n` 切分后，若直接丢弃分隔符，相邻段落文本会粘连。通过 `[p + sep for p in parts[:-1]] + [parts[-1]]` 将分隔符保留在片段末尾。

**overlap 自适应裁剪**：当 `chunk_overlap ≥ chunk_size` 时会产生死循环。通过 `overlap_size = min(chunk_overlap, max(0, chunk_size - len(new)))` 自适应裁剪，保证 overlap 部分加上新片段始终不超过 chunk_size。

### 7. ParentChild 两阶段检索架构

参考 Dify Parent-Child Retrieval 设计，解决"检索精度"与"上下文质量"之间的内在矛盾：

```
文档
  └─ 父块（512 token）→ 喂给 LLM，提供完整上下文
        └─ 子块（128 token）→ 向量化入库，精准定位

查询 → 向量检索子块（精准匹配） → 取出对应 parent_text → 送给 LLM
```

### 8. Embedder 两级批处理设计

- **BaseEmbedder**（count 级）：按 `batch_size` 条数分批，保证单次 API 调用不超过 OpenAI 的条数上限
- **OpenAIEmbedder**（token 级）：在每批内再按累计 token 数（≤ 300K/次）二次分批，防止超大 table chunk 撑爆单次请求

同时，单条文本超过模型 token 上限（8191）时自动截断而非报错。

### 9. 精细化重试策略

重试逻辑区分四种情况：

| 异常类型 | 处理方式 |
|---|---|
| `RateLimitError`（HTTP 429） | 指数退避重试 |
| `APIStatusError`（HTTP 5xx） | 指数退避重试 |
| `APIConnectionError`（网络异常） | 指数退避重试 |
| `APIStatusError`（HTTP 4xx，非 429） | 直接抛出，不重试 |

关键细节：`RateLimitError` 是 `APIStatusError` 的子类，需在 4xx 直接抛出的判断中显式排除，否则 429 会被误判为客户端错误而跳过重试。

---

## 混合检索（Step 2）

### 10. Milvus Schema：顶层字段 + Partition Key 多租户隔离

高频过滤字段（`source_file`、`content_type`）提升为顶层 VARCHAR 字段并建 INVERTED 索引，其余非结构化 metadata 存入 `extra_meta` JSON。按 `knowledge_base_id` 作为 Partition Key 分区，天然支持多知识库数据隔离，同时规避 Dify 的 Collection-per-dataset 方案在 Milvus 10K collection 上限的扩展瓶颈。

### 11. 混合检索：Weighted Sum 而非 RRF，保留双路原始分数

RRF 只使用排名、丢弃原始分数，无法反映"某路完全没命中"的情况。AgentWeave 选择 **Weighted Sum**：

```
fusion_score = α × norm(vector_score) + (1-α) × norm(bm25_score)
```

`RetrievedChunk` 同时携带 `vector_score`、`bm25_score`、`fusion_score`、`rerank_score` 四个字段，无需重查库即可在日志和调试界面中定位问题。归一化采用 **Query-level min-max**（在本次查询候选集内部计算），而非全局归一化——BM25 分数无上界，全局 min/max 无意义。

### 12. 双路检索并发，不串行等待

- **同步路径**：模块级 `ThreadPoolExecutor(max_workers=2)`，一个 worker 跑 Vector，一个跑 BM25，总耗时降至较慢一路
- **异步路径**：`aretrieve()` 用 `asyncio.gather` 并发两路，FastAPI 路由无阻塞

线程池为模块级常量，不在每次调用时重建，避免线程池创建开销。

### 13. Milvus 内置 BM25 Function，零摄入改造

BM25 使用 Milvus 2.5 的内置 Function，在 insert 时自动将 `text` 转为稀疏向量，查询时同样自动转换。Python 侧只传原始字符串，摄入 pipeline 完全不动。对比 Python 侧 `BM25EmbeddingFunction`：需要预计算稀疏向量并修改摄入 pipeline，且线上/线下模型不一致时会产生检索偏差。内置 Function 从根本上消除了这一风险。

---

## RAG Chain + 前端（Step 3）

### 14. 单次 LangGraph 执行同时流式输出 token 和引用

朴素实现会调用两次 LangGraph（一次 stream tokens，一次 invoke 取引用），消耗双倍 LLM 费用。AgentWeave 的 `astream_full()` 使用 `astream_events(version="v2")` 在单次 graph 执行中：
- 捕获 `on_chat_model_stream` 事件 → 产出 `("token", str)` 给前端逐字渲染
- 捕获 `on_chain_end` 事件（含完整 GraphState）→ 产出 `("result", dict)` 含引用元数据

SSE 路由消费此 async generator，每次 yield 前 `await request.is_disconnected()` 检测断线，`asyncio.CancelledError` 显式 re-raise，`[DONE]` 在 `finally` 块保证必达。

### 15. 引用格式选型：`[N]` + regex 提取

System prompt 约束引用编号必须放在**句号之前**（"…内容 [1]。"格式），regex 提取 + 出界编号过滤已足够可靠。ContextBuilder 采用 Dify 的 `<context>` XML 标签隔离注入内容，并在其中嵌入 RAGflow 风格的 `[N] 来源：file | section` 前缀，兼顾结构清晰与引用追踪。

### 16. fetch + ReadableStream 替代 EventSource，支持 POST + 主动取消

浏览器原生 `EventSource` 只支持 GET 请求，无法携带 JSON body，且不支持 `AbortController` 取消。AgentWeave 直接使用 `fetch` + `ReadableStream`：约 40 行，零额外依赖。停止生成时调用 `AbortController.abort()`，后端通过 `await request.is_disconnected()` 检测断线停止 LLM 流式输出。

### 17. requestAnimationFrame 批量 token 合并，避免每 token 触发 setState

使用 `tokenBufRef`（`useRef<string>`）暂存收到的 token，配合 `rafRef`（`useRef<number>`）做 `requestAnimationFrame` 调度：只在浏览器下一帧渲染前才将缓冲区 flush 到 `setState`。将 setState 调用次数从 O(token数) 降至 O(帧数，约 60fps)。

### 18. 智能自动滚动：用户上翻时停止跟随，回到底部按钮

`onScroll` 事件实时计算 `scrollHeight - scrollTop - clientHeight`，距底部 `< 120px` 时标记 `isNearBottom=true`。只有 `isNearBottom` 时才执行自动滚动，流式输出期间使用 `behavior: "instant"` 避免平滑滚动动画造成视觉抖动。

### 19. ReactMarkdown 自定义渲染器实现内联引用跳转，不引入 rehype-raw

在 ReactMarkdown 的 `components` 中自定义 `p` 和 `li` 的渲染函数，递归遍历 React children，将匹配 `/\[(\d+)\]/g` 的文本节点拆分为普通文本 + `<sup><button>` 引用元素。全程纯 React 节点操作，无 HTML 字符串注入，无额外依赖。

---

## Auth + 知识库管理（Step 4）

### 20. 文件上传竞态条件修复：先建记录再入队

上传接口朴素实现先 `ingest_document.delay()` 再 `create_document()`，但 Celery Worker 在高并发下会在 DB 记录创建前就开始执行任务，通过 `task_id` 反查文档时得到 `None`，导致状态更新全部变成 no-op，文档永远停在 pending。

AgentWeave 反转执行顺序：`create_document()` 先写入 DB 拿到 `doc_id`，再将 `doc_id` 直接作为参数传入 `ingest_document.delay(doc_id=...)`，Worker 无需反查数据库，从根本上消除竞态。

### 21. Celery 摄入重试：仅末次失败标 ERROR，重试中保持 PROCESSING

通过 `self.request.retries >= self.max_retries` 判断是否为最终失败，中间重试保持 PROCESSING 状态不变，仅在用尽所有重试次数后才写 ERROR。前端看到的状态始终与任务生命周期语义一致。

### 22. 软删除 + 幂等异步清理，保证外部数据源最终一致

硬删除知识库时若同步清理 Milvus，一旦 Milvus 超时，整个 HTTP 请求失败，但 PostgreSQL 记录已删，产生孤立的向量数据。AgentWeave 对 `KnowledgeBase` 和 `Document` 使用软删除，API 立即返回，异步 Celery 任务负责清理 MinIO 对象和 Milvus chunks。清理流程幂等——Celery retry 重跑时不产生虚假报错。

### 23. TOCTOU 竞态修复：register 依赖 DB 唯一约束而非先查后写

直接 `INSERT`，捕获 SQLAlchemy `IntegrityError` 后 `rollback()` 并转换为 `ValueError`，依赖数据库 UNIQUE 约束作为唯一事实来源，彻底消除先查后写的 TOCTOU 竞态。

---

## 工具体系（Step 5）

### 24. 工具 Schema 单一来源：BaseTool.to_function_schema() 统一生成

工具参数描述在两个地方都需要用到：① 发给 LLM 的 Function Calling JSON；② `/api/tools/` 调试接口返回的参数文档。`BaseTool.to_function_schema()` 是唯一的 schema 生成入口。关键细节：只移除顶层 `title/description`，**保留 `$defs`**——嵌套模型和 Enum 的 `$ref` 引用依赖它，OpenAI 能正确解析复杂参数结构。

### 25. 工具执行引擎：参数校验 → Redis 缓存 → asyncio 超时三级保护

`ToolExecutor.execute()` 实现了三级保护链：

1. **参数校验**：通过工具自身的 Pydantic args_schema 校验，类型错误立即返回 `ToolResult(is_error=True)`，不到达执行层
2. **Redis 缓存**：key = `tool:{name}:{sha256(sorted_json(args))[:16]}`，命中直接返回，错误结果不写缓存（避免缓存错误状态）
3. **asyncio 超时**：`asyncio.wait_for(tool._arun(), timeout=tool.timeout)` 保护，超时返回结构化错误而非抛异常

---

## 三层记忆系统（Step 6）

### 26. 统一全局配置：单文件 Settings 替代分散的 os.environ

`backend/config.py` 将全部配置合并为单个 `Settings(BaseSettings)` 类，`load_dotenv()` 在 import 时调用（优先于 `Settings` 实例化）。`AliasChoices` 兼容旧 `RAG_*` / `API_*` 前缀，存量 `.env` 无需改动即可直接升级。

### 27. 三层记忆分离：AsyncPostgresSaver / Milvus / PostgreSQL+Redis 各司其职

| 层 | 存什么 | 技术 | 读写时机 |
|---|--------|------|---------|
| 短期（Working） | 当前会话消息 | LangGraph AsyncPostgresSaver | 实时读写，与主数据库共用同一 PG 实例 |
| 长期（Episodic） | 历史对话摘要向量 | Milvus `memory_summaries` | 会话结束后异步写，新对话开始时语义检索读 |
| 语义（Semantic） | 用户偏好 / 常用话题 | PostgreSQL + Redis 缓存 | 会话结束后异步提取，对话开始时读取注入 |

短期记忆使用 LangGraph 的 AsyncPostgresSaver，thread_id = `{user_id}:{session_id}`，与 HITL 持久化共享同一 checkpointer，无需维护两套存储。

### 28. 摘要压缩 + 原地裁剪：checkpoint 直接改写

会话消息超过 20 条时，`ShortTermMemory.compress()` 完成两件事：① 调用 LLM 生成 150–300 字摘要；② 将 checkpoint 中的旧消息**原地替换**为 `[SystemMessage("[历史摘要] …")] + 最近 6 条`，防止上下文无限膨胀。`WeakValueDictionary[str, asyncio.Lock]` 保证同一会话的并发 compress 调用串行执行，Lock 在会话不再被引用时自动回收，无需手动清理。

### 29. Recency Bias + 分数过滤：历史记忆按时间升序注入

LongTermMemory 语义检索后，做两道后处理：① 分数过滤（默认阈值 0.5），过滤低相关记忆；② 时间升序排列，利用 LLM 对上下文尾部的 Recency Bias——最近的记忆距当前问题最近，LLM 更容易优先参考。

### 30. with_structured_output 替代手动 JSON 解析：LLM 结构化提取用户画像

`UserProfileManager.extract_and_update()` 用 `ChatOpenAI.with_structured_output(_ExtractedProfile)` 直接获得 Pydantic 对象，所有字段均为 `Optional`（无法判断时返回 `None`，不猜测），再用 `model_dump(exclude_none=True)` 提取有效字段做 upsert。`preferences` 字段做 dict merge，`frequent_topics` 去重追加保留最近 20 个，保证画像随使用持续积累而非被覆盖。

---

## Multi-Agent + HITL（Step 7）

### 31. Supervisor 三重防幻觉：上下文窗口 + 内容截断 + 硬路由防火墙

LLM 在 Agent 协作中容易产生三类幻觉：上下文爆炸、无限循环路由、提前宣布结束。AgentWeave 在 `supervisor.py` 中对三类问题各施对策：

1. **上下文窗口裁切**（`SUPERVISOR_CONTEXT_WINDOW=6`）：Supervisor 每次调用只看最近 6 条消息，防止长对话中上下文爆炸影响路由决策
2. **消息内容截断**（800 chars/message）：Worker 输出过长时截断再注入，防止 Analyst 的详细工具结果撑爆 Supervisor 上下文
3. **硬路由防火墙**：代码层检测连续相同路由（Researcher/Analyst 重复调用）→ 强制转向 Reporter；检测过早 `__end__` → 恢复路由

防火墙不依赖 LLM 判断，在条件边函数中直接实施，LLM 无法绕过。

### 32. Supervisor 路由：with_structured_output 三字段决策，非 tool_calls

Supervisor 不使用 `tool_calls` 路由（这会要求 LLM 选择调用哪个"路由工具"），而是用 `with_structured_output(RouterDecision)` 直接获得三个字段：

```python
class RouterDecision(BaseModel):
    next_agent: Literal["researcher", "analyst", "executor", "reporter", "__end__"]
    current_task: str        # ≤100 字，传递给下一个 Worker 的任务说明
    message_to_user: str     # 前端展示的状态提示
```

结构化输出相比 tool_calls 的优势：① 字段有 Literal 类型约束，LLM 不能输出非法值；② `current_task` 字段让 Supervisor 向 Worker 传递精确任务说明，而非每次 Worker 都从整个 messages 历史推断；③ 调试时三个字段完整可见，不需要解析 tool_calls。

### 33. Agentic RAG：评分快速通道 + 跨迭代最优保留

Researcher 自校正检索（CRAG 思路）引入了两个关键工程细节：

**评分快速通道**：检索结果的 rerank_score ≥ 0.72 时直接跳过 LLM 评估（`docs_relevant=True`），不消耗额外 token。只有分数处于灰色地带（0.5–0.72）才走 LLM 评估。

**best_retrieved_docs 跨迭代保留**：在最多 3 次改写循环中，`best_retrieved_docs` 始终记录历史最佳检索结果。若某次改写后召回率反而下降（零命中或分数下降），用 best_retrieved_docs 兜底而非返回空结果。

### 34. Analyst 写操作拦截：read-only 执行模式 + 信令协议

Analyst 的 ReAct 循环绑定了所有 MCP 工具，但在**研判阶段**不允许执行写操作（写操作需先经过 HITL 审批）。AgentWeave 通过工具描述检测（含"需经 HITL"字样）在执行前拦截写操作，并输出结构化信令：

```
【HITL_REQUIRED】<操作描述，供前端展示>
【EXECUTION_INTENT】{"tool_name": "...", "params": {...}}
```

Supervisor 解析这两个 marker，将 `execution_intent` 写入 state，触发 HITL 节点。这一设计让 Analyst 作为"意图发现者"，Executor 作为"安全执行者"，两者职责完全分离，无需共享代码。

### 35. Executor 确定性执行："auto" 参数运行时解析，无 LLM ReAct

Executor 节点不是另一个 ReAct 循环，而是**确定性执行**：直接读取 state 中的 `execution_intent`（tool_name + params），不做任何 LLM 推理，直接调用对应工具。

"auto" 参数占位符处理：如 `ambulance_id="auto"` 表示"任意可用车辆"。Executor 在工具调用前进行参数归一化：`"auto"` → 运行时根据实际状态解析为具体值（如 `"A3"`）。这避免了 LLM 在生成意图时不知道当前系统状态而瞎猜具体 ID。

### 36. HITL interrupt()：LangGraph 原生暂停，跨重启持久化

高风险操作触发 `interrupt(payload)` 时，LangGraph 将 graph 状态持久化到 PostgreSQL（AsyncPostgresSaver，与主数据库共用），SSE 连接立即关闭。前端收到 `interrupt` 事件后弹出审批 UI。用户审批后调用 `/agent/resume` 接口，LangGraph 从持久化 checkpoint 恢复执行——即使期间服务器重启，审批状态也不会丢失。

### 37. @mention 直接调度：绕过 Supervisor 路由 + 跳过 HITL

用户消息以 `@Agent名` 开头时，后端在入 graph 前直接将该 Agent 设为 `next_node`，跳过 Supervisor 的 LLM 路由判断，同时跳过对应的 HITL 拦截。直接指名的操作默认已经过用户确认，无需再次审批。这一设计让有经验的用户可以精确控制执行路径，同时不影响普通用户的自动路由体验。

---

## 层级 Agent 网络（Step 9）

### 38. 两级 Supervisor 架构：数据不出域，结论跨域流动

**部门级 Supervisor**（`supervisor.py`，每个 AgentWeave 实例独立运行）：路由本部门内部的 Analyst / Researcher / MCP 工具，专注单一领域，输出结构化 JSON 给上级。

**Weave Supervisor**（`weave_supervisor.py`）：不路由本地 Agent，只通过 A2A HTTP 调用已注册的部门 Agent，聚合所有部门报告后制定执行计划，关键节点触发 HITL 审批。每个部门的数据和工具完全不出域，Weave Supervisor 只接触聚合后的结论。

### 39. A2A 协议：AgentCard 自描述 + 标准化推理接口

每个 AgentWeave 实例在 `/.well-known/agent.json` 暴露 AgentCard（名称、能力描述、支持的任务类型）。Weave Supervisor 通过 URL 拉取 AgentCard 完成 Agent 注册，无需提前配置 schema。**Weave 还利用 AgentCard 的能力描述动态生成部门任务**：将 AgentCard 能力字段传入 LLM，让 LLM 根据事故信息生成最适配该部门能力的任务说明，而非硬编码任务模板。

### 40. Weave 四类意图分类 + 多路快速通道

每轮用户输入首先经过意图分类，决定进入哪条执行路径：

| 意图 | 触发条件 | 执行路径 |
|------|---------|---------|
| `incident_response` | 首轮对话，或含城市范围关键词 | 全量多部门研判 → 计划 → HITL → 执行 |
| `direct_command` | `@部门名` 开头，或 LLM 识别为单步指令 | `create_single_step_plan` → 可选 HITL → 执行 |
| `follow_up` | LLM 识别为追问 | 直接到 final_report，不重新调度 |
| `escalation` | 超出单部门范围 | 强制走 `phase_dispatch` 全量路径 |

首轮对话 Turn 0 直接路由为 `incident_response`，不调用 LLM 分类，节省一次 token 消耗。

### 41. 位置消歧 HITL-0：AMap 文字搜索 + 候选确认中断

Weave Supervisor 在开始多部门研判前，先通过高德 MCP 文字搜索将用户输入的地址（如"港城大道388号"）解析为多个候选地点（含坐标），触发 `interrupt(location_candidates=...)` 让用户确认。

Resume 时接受三种值：`dict`（用户选定的候选地点，含 lat/lng）、`"search:新关键词"`（用户要求重新搜索）、其他（跳过定位直接继续）。确认后的坐标持久化到 `WeaveState.incident_lat/lng`，后续所有地图渲染和路径规划都以此为基准。

### 42. A2A 差异化重试：连接错误重试，超时/业务失败直接标 failed

A2A 调用的错误性质不同，重试策略也应不同：

- **连接错误**（`aiohttp.ClientConnectorError`）：网络抖动或部门服务刚启动，值得重试（最多 2 次）
- **超时**（`asyncio.TimeoutError`，默认 120s）：部门 Agent 计算耗时超限，重试大概率还是超时，直接标 `status=timeout`
- **业务失败**（部门返回 `status=failed`）：说明部门 Agent 内部有问题，重试无意义，直接透传 failed

对于 `timeout` 和 `failed` 的部门，Weave Supervisor 触发 HITL（研判失败审查），让指挥长决定是继续剩余部门的方案还是中止整个应急响应。

### 43. 计划场景规则兜底：LLM 忘写 execution_tool 时代码层注入

LLM 生成执行计划时偶尔会遗漏 `execution_tool` 字段（该字段标识对应的 MCP 写操作工具）。AgentWeave 在计划生成后做一轮代码层修正：

```python
SCENE_RULES = {
    "medical_ems":        "dispatch_ambulance",
    "fire_brigade":       "dispatch_fire_trucks",
    "traffic_control":    "apply_evacuation_plan",
    "emergency_supplies": "allocate_standard_pack",
}
```

对于 `is_high_risk=True` 但 `execution_tool` 为空的步骤，根据 `dept_code` 从 `SCENE_RULES` 查找默认工具并注入。这比要求 LLM 每次都完美填写更可靠，将领域知识从 prompt 下沉到代码，减少 prompt 膨胀。

### 44. 事故中心三级推断：污染源 > ERPG 圆心 > 传感器质心

执行阶段需要精确的事故地理中心用于路线规划和地图渲染。Weave Supervisor 通过 `_derive_incident_center()` 按优先级推断：

1. **污染源标记**：环保局研判报告中由 env_agency 标注的泄漏源 Marker（精度最高）
2. **ERPG 扩散圆心**：环保局返回的 ERPG 影响圆的圆心坐标
3. **传感器标记质心**：所有研判阶段 Marker 的几何中心（当无法从环保局获取精确来源时）

最终兜底：使用位置消歧阶段确认的 `incident_lat/lng`。四级优先级确保即使个别部门返回数据不完整，地图渲染也不会崩溃。

### 45. 路线合成兜底：Weave 直调高德路径规划

部门 Agent 在执行阶段理论上应返回路线数据（救护车行驶路线等）。但当部门 Agent 未返回路线时，Weave Supervisor 不报错，而是通过 `_resolve_route_endpoints()` 从研判阶段的 Marker 数据中推断起终点（如医院位置 → 事故地心），再直接调用高德驾车路径规划 MCP 获得路线，将结果作为 `map_update` 推送到前端。

这一兜底逻辑保证了地图上路线渲染的完整性，无论哪个部门的数据质量如何。

### 46. A2A Server 全自动 HITL 代批：审批权在上游 Weave 层

部门 A2A Server 内部运行完整的部门级 Agent 图，其中包含 HITL 节点。但在 A2A 模式下，由于操作是由 Weave Supervisor 触发的，审批责任在 Weave Coordinator 那一层。因此部门 A2A Server 对所有 `interrupt()` 信号自动 `resume("approve")`，让部门 Agent 图继续执行。

这消除了跨层审批的复杂性：Weave HITL 审批计划层面，部门 Agent 专注执行。两层之间职责清晰，不产生审批嵌套。

### 47. 指挥中心 SSE 事件体系：20+ 类型，三层展示联动

Weave Supervisor 定义了完整的 SSE 事件分类体系，每类事件映射到特定 UI 层：

| 事件类型 | 触发时机 | 展示层 |
|----------|---------|--------|
| `research_dispatch` | 多部门任务分配完成 | 指挥面板：orch_reasoning + dept 研判卡 |
| `dept_report` × N | 各部门 A2A 返回（并行） | 指挥面板：in-place 更新对应部门卡 |
| `dispatch_plan` | 聚合计划生成 | 指挥面板：Gantt 执行计划卡 |
| `interrupt(plan_review)` | HITL-1 触发 | HITLBar：计划审批条 + PlanEditModal |
| `interrupt(location_select)` | HITL-0 触发 | LocationPickerBar：地址候选确认 |
| `interrupt(research_failure)` | 部门超时/失败 | HITLBar：继续/中止决策 |
| `plan_step(running/done/failed)` | 步骤执行状态 | 指挥面板：执行卡 + Kanban 看板 |
| `map_update` | 任何地理数据产生 | 高德地图：Marker/Polyline 增量渲染 |
| `direct_dispatch` | @mention 快速路径 | 指挥面板：确认卡 |
| `final_answer` | 全流程结束 | 指挥面板：综合报告卡 |

事件体系的层次化设计保证了前端无需轮询，所有状态完全由事件驱动更新。

### 48. HITL 三级审批：位置确认 + 计划审批 + 高危步骤逐一

Weave 应急场景中的 HITL 分三个层次，每层审批粒度递进：

- **HITL-0（位置消歧）**：在执行任何操作前，确认事故地理坐标。最终触发高德 MCP 文字搜索，候选列表由 `LocationPickerBar` 展示
- **HITL-1（计划级审批）**：生成执行计划后暂停，指挥长通过 `PlanEditModal` 可编辑步骤标题/跳过特定步骤，再提交完整 `PlanStep[]` 数组恢复执行
- **HITL-2（步骤级审批）**：`hitl_bulk_highrisk` 对 `is_high_risk=True` 的步骤逐一 `interrupt()`，每步单独弹出 HITLBar。approve 触发 `resumeWeave` → 新 SSE 流 → 该步骤开始执行，reject 触发跳过

三级 HITL 由同一个 `HITLBar` 组件处理不同 payload 类型。前端通过 `hitlPendingRef.current` 防止 `done` 事件在任一 HITL 审批期间误清审批状态。

### 49. AMap 图层分类：10+ 图层类型按事件 key 增量渲染

高德地图不是在一次性加载所有地理数据，而是随 SSE 事件增量渲染。`AMapPanel.tsx` 定义了 10+ 个命名图层：

```
incident_source   → 泄漏源红色标记
sensors           → 传感器黄色标记
resources         → 资源点（医院/消防站）蓝色标记
plume             → 扩散影响圈（半透明红色填充）
cordon            → 警戒线圆圈
signals           → 路口信号灯控制点
ambulance_route   → 救护车路线（绿色）
fire_route        → 消防路线（红色）
evacuation_route  → 疏散路线（橙色）
supply_route      → 物资配送路线（蓝色）
```

每个 `map_update` 事件携带 `layer_key`，前端根据 key 选择对应图层更新。图层对象存储在 `mapObjectsRef` 中，重复更新时先清除旧对象再渲染新对象，不产生图层叠加。
